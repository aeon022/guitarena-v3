---
const supporters = [
  { name: 'Kelag Plus', logo: 'supporters_kelag-plus-neu.webp', width: 709, height: 708 },
  { name: 'Kulturamt Spittal', logo: 'supporters_Kulturamt_Spittal.webp', width: 323, height: 186 },
  { name: 'Schloss Porcia', logo: 'supporters_L1_Schloss-Porcia.webp', width: 945, height: 1417 },
  { name: 'Land Kärnten', logo: 'supporters_Land_Kaernten.webp', width: 593, height: 130 },
  { name: 'Musik Aktiv', logo: 'supporters_Musik_Aktiv.webp', width: 502, height: 226 },
  { name: 'Volksbank', logo: 'supporters_Volksbank.webp', width: 605, height: 116 },
];
---

<section class="py-24 border-t border-orten-border overflow-hidden bg-orten-bg">
  <div class="max-w-6xl mx-auto px-8 md:px-16 mb-16 text-center">
    <h2 class="text-4xl font-serif text-orten-text mb-4">Partner & Unterstützer</h2>
    <p class="font-mono text-[10px] uppercase tracking-[0.3em] text-orten-muted">Gemeinsam für die Kultur in Spittal</p>
  </div>

  <div class="group relative w-full inline-flex flex-nowrap overflow-hidden [mask-image:_linear-gradient(to_right,transparent_0,_black_128px,_black_calc(100%-128px),transparent_100%)]">
    <ul class="flex items-center justify-center md:justify-start [&_li]:mx-8 md:[&_li]:mx-12 animate-infinite-scroll group-hover:[animation-play-state:paused]">
      {supporters.map(supporter => (
        <li class="flex-shrink-0 w-24 md:w-32 transition-all duration-700 hover:scale-110 group">
          <img 
            src={import.meta.env.BASE_URL + `images/supporters/${supporter.logo}`} 
            alt={supporter.name}
            width={supporter.width}
            height={supporter.height}
            loading="lazy"
            decoding="async"
            class="w-full h-auto object-contain grayscale opacity-40 group-hover:opacity-100 transition-all duration-700"
          />
        </li>
      ))}
    </ul>
    <!-- Duplicate for seamless loop -->
    <ul class="flex items-center justify-center md:justify-start [&_li]:mx-8 md:[&_li]:mx-12 animate-infinite-scroll group-hover:[animation-play-state:paused]" aria-hidden="true">
      {supporters.map(supporter => (
        <li class="flex-shrink-0 w-24 md:w-32 transition-all duration-700 hover:scale-110 group">
          <img 
            src={import.meta.env.BASE_URL + `images/supporters/${supporter.logo}`} 
            alt={supporter.name}
            width={supporter.width}
            height={supporter.height}
            loading="lazy"
            decoding="async"
            class="w-full h-auto object-contain grayscale opacity-40 group-hover:opacity-100 transition-all duration-700"
          />
        </li>
      ))}
    </ul>
  </div>
</section>

<style>
  /* Local overrides if needed */
</style>
