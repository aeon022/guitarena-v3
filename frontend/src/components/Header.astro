---
const navItems = [
  { label: 'Home', href: import.meta.env.BASE_URL },
  { label: 'Programm', href: import.meta.env.BASE_URL + 'programm' },
  { label: 'Archiv', href: import.meta.env.BASE_URL + 'archiv' },
  { label: 'Über Uns', href: import.meta.env.BASE_URL + 'about' },
];
---

<header class="sticky top-0 z-50 bg-brand-bg/80 backdrop-blur-md border-b border-brand-dark px-4 py-4">
  <div class="container mx-auto max-w-5xl flex justify-between items-center">
    <a href={import.meta.env.BASE_URL} class="text-xl font-bold tracking-tighter text-brand-primary">
      GUITARENA<span class="text-brand-text">.AT</span>
    </a>
    
    <nav>
      <ul class="flex gap-6 md:gap-10">
        {navItems.map(item => (
          <li>
            <a 
              href={item.href} 
              class="text-sm font-medium hover:text-brand-primary transition-colors duration-200"
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
  </div>
</header>
